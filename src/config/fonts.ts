// Since we're not using Next.js anymore, we'll handle fonts differently
// For now, we'll use CSS variables that can be set in globals.css

export const fontSans = {
  variable: "--font-sans",
  className: "font-sans"
};

export const fontMono = {
  variable: "--font-mono",
  className: "font-mono"
};